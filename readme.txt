gcc version 9.4.0 (Ubuntu 9.4.0-1ubuntu1~20.04.1)

Use command "make" to build binary file named m11152013
Use command "make clean" to delete binary file and Boolean functionality of each node in a text file("function.out")


Use command "./m11152013" and input one *.blif file to run.
	And then it will output the Boolean functionality of each node in a text file("function.out")

Execution:Inputting a node x will show its predecessor and successor until inputting 0 to shut it down.